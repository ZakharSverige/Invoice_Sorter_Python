# 📦 Python Скрипт: Автоматизация Логистики Документооборота

## Introduction
Этот проект демонстрирует использование **Python** и **библиотек для работы с данными** для решения реальной бизнес-задачи — **автоматизированной сортировки документов**. Фокус проекта — на повышении операционной эффективности и минимизации ручного труда.

🔍 Исходный код скрипта: [invoice_sorter.py](/invoice_sorter.py)

---

## Background
Проект был инициирован с целью устранения значительных ручных трудозатрат (до **3 рабочих дней**), необходимых для сортировки платежной документации по курьерам. Автоматизация процесса позволила высвободить операционный персонал для выполнения более сложных задач, а также обеспечила **высокую точность** маршрутизации.

### ❓ Ключевые Цели Проекта:

1.  Сократить время сортировки документов **с 3 дней до 15 минут**.
2.  Автоматически сопоставлять адресные данные из имен PDF-файлов с мастер-данными курьеров.
3.  Разработать надежный скрипт, пригодный для использования персоналом.
4.  Создать папку для файлов, которые не удалось сопоставить, для ручной проверки.

---

## 🛠️ Tools I Used
Для реализации этого решения были использованы следующие инструменты:

* **Python 3.x:** Основной язык программирования.
* **`pandas`:** Для эффективной загрузки, обработки и создания словаря сопоставлений из **CSV-справочника**.
* **`os` и `shutil`:** Системные библиотеки, критически важные для навигации по файловой системе и физического **перемещения** файлов.
* **Visual Studio Code:** Использовался как основная среда разработки.
* **Git & GitHub:** Для версионирования и размещения готового кода.

---

## 💻 Код и Логика (The Code and Logic)

Этот скрипт выполняет последовательную логику, управляя данными и файловой системой:

### 1. Загрузка Данных и Кэширование (Фрагмент `pandas`)

Данные курьеров загружаются из CSV-файла и преобразуются в словарь Python (`courier_mapping`) для **мгновенного поиска** (O(1) — константное время).

```python
# --- Фрагмент кода загрузки и кэширования данных ---
# Создаем словарь для быстрого поиска: 'Андропова д.2' -> 'Иванов Иван'
courier_mapping = {
    row['Адрес'].replace(' д.', '_').replace('.', ''): row['ФИО']
    for index, row in courier_df.iterrows()
}
# ...
### 2. Главная Логика Сортировки и Перемещения (Фрагмент `shutil`)

Скрипт итерирует по папкам с почтовыми индексами. На каждом шаге он извлекает адрес из имени файла, ищет его в словаре и перемещает файл в соответствующую папку.

```python
# --- Фрагмент кода сортировки и маршрутизации ---
address_from_filename = parts[1].rsplit('.', 1)[0].replace('.', '')
courier_name = courier_mapping.get(address_from_filename)

if courier_name:
    # Перемещение файла в папку курьера
    target_courier_folder = os.path.join(root_data_folder, courier_name)
    os.makedirs(target_courier_folder, exist_ok=True)
    shutil.move(full_file_path, destination_path) 
else:
    # Перемещение файла в папку для нераспознанных
    shutil.move(full_file_path, os.path.join(unmatched_path, filename))
